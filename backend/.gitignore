# --- Node / Fastify ---
node_modules/
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Testing & coverage
coverage/
.nyc_output/

# TypeScript build output & caches
dist/
build/
out/
.tmp/
temp/
*.tsbuildinfo

# Lint/format caches
.eslintcache

# --- Environment variables ---
# Never commit real secrets; ship only examples
.env
.env.*
prisma/.env
!.env.example
!.env.*.example

# --- Prisma ---
# Keep migration history in Git (required for deploys)
!prisma/migrations/
# Ignore local SQLite DBs used for dev
prisma/*.db
prisma/*.db-journal

# --- Yarn / npm ---
.pnpm-store/
.pnp.*
.npm/

# Yarn Modern (Berry) patterns — safe even if you’re not using them
.yarn/*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# --- Editors & OS cruft ---
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# --- Docker (optional local overrides/volumes) ---
docker-compose.override.yml
.docker/
docker-data/
*.local.yml

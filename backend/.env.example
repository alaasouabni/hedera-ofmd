# =================================================================
# Chain & Contract Configuration
# =================================================================
# Hedera EVM RPC endpoint for testnet
EVM_RPC=https://testnet.hashio.io/api
# Address of the MintingHub smart contract
MINTING_HUB=your_minting_hub_address
# Block number where MintingHub was deployed (for event indexing), do not set to 0 or you will hit ip rate limits from the public rpc
MINTING_HUB_DEPLOY_BLOCK=0

# =================================================================
# Block Scanning Configuration
# =================================================================
# Maximum number of blocks to scan backwards for historical events
BACKSCAN_BLOCKS=250000
# Number of blocks to process in a single chunk when fetching logs
LOG_CHUNK_SIZE=2000
# Delay between API requests to respect rate limits (in milliseconds)
RATE_LIMIT_DELAY_MS=250
# Maximum number of retry attempts for failed requests
MAX_RETRIES=5
# Delay between polling for new blocks (in milliseconds)
THROTTLE_BLOCK_POLL_MS=4000
# Delay between processing batches of transaction details (in milliseconds)
DETAILS_BATCH_DELAY_MS=50

# =================================================================
# Database Configuration
# =================================================================
# PostgreSQL connection string
# Format: postgresql://<user>:<password>@<host>:<port>/<database>?schema=<schema>
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ofd_indexer?schema=public

# =================================================================
# Server Configuration
# =================================================================
# Port number for the backend server
PORT=4000
# Host address to bind the server to (0.0.0.0 allows external connections)
HOST=0.0.0.0
# Secret key for JWT token generation and validation
# IMPORTANT: Change this to a secure random string in production
JWT_SECRET=your_secure_jwt_secret
# Allowed origin for CORS (frontend application URL)
FRONTEND_ORIGIN=http://localhost:5173
# Address of the Voucher Module smart contract
VOUCHER_MODULE=your_voucher_module_address
# Block number where VoucherModule was deployed (for event indexing), do not set to 0 or you will hit ip rate limits from the public rpc
VOUCHER_DEPLOY_BLOCK=0